{
  "name": "dead-code-detector",
  "version": "1.0.0",
  "description": "AST-based dead code detection for TypeScript/JavaScript",
  "type": "module",
  "main": "detect.ts",
  "scripts": {
    "detect": "bun run detect.ts",
    "test": "bun run detect.ts ."
  },
  "keywords": ["dead-code", "ast", "typescript", "javascript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ast-grep/napi": "^0.20.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "bun-types": "^1.0.0"
  }
}
